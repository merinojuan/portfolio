---
// import Background from '@components/Background.astro';
interface Props {
	name: string;
}

const { name } = Astro.props;
---

<div class="inline-block w-16 h-16 md:w-28 md:h-28">
  <!-- <div class="atropos experience-item-atropos w-full h-full">
    <div class="atropos-scale">
      <div class="atropos-rotate">
        <div class="atropos-inner rounded-full ring-1 ring-gray-800/10 dark:ring-gray-200/5">
          <div class="background-content bg-base-200" data-atropos-offset="0">
            <Background class="background-content__svg" atroposoffset="-5" />
          </div>
          <div class="data-content backdrop-blur-sm" data-atropos-offset="0">
            <div class="flex flex-col items-center justify-center h-full w-full">
              <slot />
              <p class="hidden md:block font-medium text-xs md:text-sm m-0 mt-1" data-atropos-offset="-12">{name}</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div> -->
  <div class="rounded-full bg-base-200 ring-1 ring-gray-800/10 dark:ring-gray-200/5 w-full h-full">
    <div class="flex flex-col items-center justify-center h-full w-full">
      <slot />
      <p class="hidden md:block font-medium text-xs md:text-sm m-0 mt-1">{name}</p>
    </div>
  </div>
</div>

<style>
.background-content {
  position: relative;
  left: -5rem;
  top: -5rem;
  width: calc(100% + 10rem);
  height: calc(100% + 10rem);
}

.data-content {
  position: absolute;
  width: 100%;
  height: 100%;
  left: 0;
  top: 0;
}
</style>
